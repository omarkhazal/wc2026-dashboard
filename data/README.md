# World Cup data cache

`wc-data.json` is generated by the GitHub Actions workflow at `.github/workflows/update-wc-data.yml`.
Do not manually edit or replace it from release ZIPs; run the workflow to refresh it.
